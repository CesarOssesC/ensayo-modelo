<%= form_with(model: boleta_mes) do |form| %>
  <% if boleta_mes.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(boleta_mes.errors.count, "error") %> prohibited this boleta_mes from being saved:</h2>

      <ul>
        <% boleta_mes.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :boleta_id, style: "display: block" %>
    <%= form.text_field :boleta_id %>
  </div>

  <div>
    <%= form.label :mes_id, style: "display: block" %>
    <%= form.text_field :mes_id %>
  </div>

  <div>
    <%= form.label :year_id, style: "display: block" %>
    <%= form.text_field :year_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
